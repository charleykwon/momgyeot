{
  "name": "momgyeot",
  "version": "4.0.0",
  "description": "맘곁 - 임신·출산·육아 AI 컴패니언",
  "private": true,
  "scripts": {
    "dev": "vercel dev",
    "build": "echo 'No build required'",
    "deploy": "vercel --prod"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.52.0",
    "@supabase/supabase-js": "^2.39.0"
  }
}
